# Sample Chat App

Barebone Rails application showing some basic usage of the [Sync](http://github.com/chrismccord/sync) gem.

## Running it

1. `rake db:create`
2. `rake db:migrate`
3. `rake db:seed`

Make sure you have a [Pusher](http://pusher.com) account and that your Pusher credentials have been setup in `config/sync.yml`.
